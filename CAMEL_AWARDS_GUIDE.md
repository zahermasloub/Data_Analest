# 🏆 دليل استخدام صفحة تحليل جوائز سباقات الهجن

## 📖 نظرة عامة

صفحة **تحليل جوائز سباقات الهجن** هي أداة متقدمة لمطابقة بيانات الجوائز مع كشوفات البنك، واكتشاف حالات الصرف المكرر، والتأكد من سلامة عمليات الدفع.

---

## 🎯 الأهداف الرئيسية

1. **مطابقة دقيقة**: مقارنة بيانات الجوائز مع سجلات التحويلات البنكية
2. **كشف التكرار**: اكتشاف حالات الصرف المكرر (المشتبه والمؤكد)
3. **ضمان الجودة**: التحقق من سلامة البيانات وصحة المعاملات
4. **تقارير شاملة**: إنشاء تقارير مفصلة قابلة للتصدير

---

## 🔧 المكتبات المستخدمة

### Backend (Python)
```python
pandas>=2.1.0          # معالجة البيانات الأساسية
polars>=0.19.0         # معالجة سريعة للبيانات الكبيرة
pyarrow>=14.0.0        # تسريع العمليات
openpyxl>=3.1.0        # قراءة وكتابة Excel
xlsxwriter>=3.1.0      # إنشاء تقارير Excel متقدمة
rapidfuzz>=3.5.0       # المطابقة الضبابية للنصوص
recordlinkage>=0.16.0  # المطابقة المركبة
pyjanitor>=0.26.0      # تنظيف البيانات
dateparser>=1.2.0      # توحيد صيغ التواريخ
pandera>=0.17.0        # التحقق من جودة البيانات
```

---

## 📥 كيفية الاستخدام

### 1. رفع الملفات

#### ملفات الجوائز (Awards Files)
- يمكنك رفع **ملف واحد أو عدة ملفات** Excel
- الملفات يمكن أن تكون من مواسم مختلفة
- الصيغ المدعومة: `.xlsx`, `.xls`, `.csv`

**الأعمدة المطلوبة (يتم توحيدها تلقائياً):**
- `اسم المالك` / `OwnerName`
- `الموسم` / `Season`
- `السباق` / `Race`
- `المبلغ` / `AwardAmount`
- `تاريخ الإدخال` / `EntryDate`

#### كشف البنك (Bank Statement)
- ارفع **ملف واحد** فقط لكشف البنك
- الصيغ المدعومة: `.xlsx`, `.xls`, `.csv`

**الأعمدة المطلوبة:**
- `اسم المستفيد` / `BankName`
- `المبلغ` / `BankAmount`
- `تاريخ التحويل` / `BankDate`
- `رقم المرجع` / `BankReference` (اختياري)

---

### 2. إعدادات التحليل

#### نافذة تطابق التاريخ
حدد المدى الزمني المسموح به بين تاريخ الجائزة وتاريخ التحويل البنكي:
- **± 3 أيام**: دقة عالية (تطابق ضيق)
- **± 7 أيام**: متوازن (مستحسن) ✅
- **± 14 يوم**: مرن (لحالات التأخير)

---

### 3. عملية التحليل

عند الضغط على زر **"ابدأ التحليل"**، يقوم النظام بـ:

#### أ) تنظيف وتوحيد البيانات
```
✓ توحيد أسماء الأعمدة
✓ إزالة المسافات الزائدة
✓ تطبيع Unicode للحروف العربية
✓ تحويل التواريخ لصيغة موحدة
✓ تحويل المبالغ لأرقام
```

#### ب) المطابقة مع كشف البنك

**الطبقة 1: Exact Matching (100%)**
```
✓ تطابق تام في المبلغ
✓ تطابق تام في اسم المالك
✓ التاريخ ضمن النافذة المحددة
```

**الطبقة 2: Fuzzy Matching (≥90%)**
```
✓ تطابق في المبلغ
✓ تطابق ضبابي في الاسم (نسبة ≥90%)
✓ التاريخ ضمن النافذة المحددة
```

#### ج) اكتشاف التكرار

**صرف مكرر مشتبه (⚠️):**
```
- نفس اسم المالك
- نفس السباق
- نفس المبلغ
- تواريخ إدخال مختلفة
```

**صرف مكرر مؤكد (❌):**
```
- تكرار رقم المرجع البنكي (BankReference)
```

---

### 4. عرض النتائج

#### شريط الإحصائيات
```
📊 إجمالي السجلات: عدد سجلات الجوائز
✅ الحالات السليمة: السجلات المطابقة بنجاح
⚠️ المشتبه: حالات تحتاج مراجعة
❌ المكرر المؤكد: تكرار مؤكد يجب معالجته
```

#### الفلاتر المتاحة
```
🔍 الموسم: فلترة حسب موسم السباق
🔍 السباق: فلترة حسب اسم السباق
🔍 الحالة: فلترة حسب الحالة (✅/⚠️/❌)
🔍 المشارك: بحث بالاسم
```

---

### 5. الرسوم البيانية

#### توزيع الحالات (Pie Chart)
- عرض نسب الحالات المختلفة
- ألوان واضحة: أخضر (✅)، أصفر (⚠️)، أحمر (❌)

#### توزيع حسب الموسم (Bar Chart)
- مقارنة الحالات عبر المواسم المختلفة
- رؤية شاملة للأداء

---

### 6. تصدير التقارير

#### التقرير الكامل (Excel)
يحتوي على 3 أوراق عمل:
1. **النتائج الكاملة**: جميع السجلات مع تفاصيلها
2. **ملخص Pivot**: تحليل تجميعي حسب الموسم والسباق
3. **الإحصائيات**: ملخص الأرقام والنسب

**التنسيق:**
- ✅ **خلايا خضراء**: حالات سليمة
- ⚠️ **خلايا صفراء**: حالات مشتبهة
- ❌ **خلايا حمراء**: تكرار مؤكد

#### النتائج المفلترة (Excel)
- تصدير السجلات المعروضة فقط بعد تطبيق الفلاتر
- مفيد للتركيز على حالات محددة

---

## 🧼 قواعد تنظيف البيانات

### 1. توحيد الأعمدة
```python
خريطة التوحيد:
'اسم المالك' → 'OwnerName'
'الموسم' → 'Season'
'السباق' → 'Race'
'المبلغ' → 'AwardAmount'
'تاريخ الإدخال' → 'EntryDate'
'اسم المستفيد' → 'BankName'
'تاريخ التحويل' → 'BankDate'
'رقم المرجع' → 'BankReference'
```

### 2. تطبيع النصوص
```python
# إزالة المسافات الزائدة
text = re.sub(r'\s+', ' ', text).strip()

# توحيد Unicode
text = text.replace('أ', 'ا').replace('إ', 'ا').replace('آ', 'ا')
text = text.replace('ة', 'ه')
text = text.replace('ى', 'ي')

# تحويل لأحرف صغيرة
text = text.lower()
```

### 3. معالجة التواريخ
```python
# توحيد صيغ التواريخ
df['Date'] = pd.to_datetime(df['Date'], errors='coerce', dayfirst=True)
```

### 4. معالجة المبالغ
```python
# تحويل النصوص لأرقام
df['Amount'] = pd.to_numeric(df['Amount'], errors='coerce')
```

---

## 🔗 قواعد المطابقة

### Exact Matching (الطبقة الأولى)
```python
if (award_amount == bank_amount) and \
   (owner_name == bank_name) and \
   (abs(entry_date - bank_date).days <= time_window):
    match_score = 100
    match_type = 'Exact Match'
```

### Fuzzy Matching (الطبقة الثانية)
```python
if (award_amount == bank_amount) and \
   (fuzzy_score >= 90) and \
   (abs(entry_date - bank_date).days <= time_window):
    match_score = fuzzy_score
    match_type = 'Fuzzy Match'
```

### حساب نسبة التطابق الضبابي
```python
from rapidfuzz import fuzz
fuzzy_score = fuzz.ratio(owner_name_normalized, bank_name_normalized)
```

---

## 🔁 آلية اكتشاف التكرار

### داخل ملفات الجوائز
```python
# تجميع حسب: الاسم + السباق + المبلغ
grouped = df.groupby(['OwnerName', 'Race', 'AwardAmount'])

for group in grouped:
    if len(group) > 1:
        dates = group['EntryDate'].unique()
        if len(dates) > 1:
            # تكرار مشتبه ⚠️
```

### من خلال كشف البنك
```python
# البحث عن تكرار في رقم المرجع البنكي
if BankReference.duplicated():
    # تكرار مؤكد ❌
```

---

## 📊 الأعمدة في التقرير النهائي

| العمود | الوصف | مثال |
|--------|--------|------|
| `OwnerName` | اسم المالك | أحمد محمد |
| `Race` | اسم السباق | كأس الملك |
| `Season` | الموسم | 2024/2025 |
| `AwardAmount` | مبلغ الجائزة | 50000 |
| `EntryDate` | تاريخ الإدخال | 2025-01-15 |
| `BankDate` | تاريخ التحويل البنكي | 2025-01-17 |
| `BankReference` | رقم المرجع البنكي | TRX-123456 |
| `MatchScore` | نسبة التطابق | 95% |
| `StatusFlag` | الحالة | ✅/⚠️/❌ |
| `ReasonText` | سبب التصنيف | مطابقة بنسبة 95% |
| `MatchType` | نوع المطابقة | Exact/Fuzzy/No Match |

---

## 💡 نصائح مهمة

### جودة البيانات
```
✓ تأكد من صحة أسماء الأعمدة في الملفات
✓ تحقق من صيغ التواريخ (يفضل: YYYY-MM-DD)
✓ تأكد من المبالغ بصيغة رقمية (بدون رموز)
✓ تجنب الخلايا الفارغة في الحقول الأساسية
```

### تحسين الأداء
```
✓ استخدم ملفات Excel حديثة (.xlsx)
✓ قم بإزالة الأعمدة غير الضرورية مسبقاً
✓ تأكد من عدم وجود صفوف فارغة
✓ استخدم أسماء واضحة للأعمدة
```

### حل المشاكل الشائعة
```
❌ "لم يتم تحميل أي ملفات"
   → تأكد من رفع الملفات بالصيغ المدعومة

❌ "خطأ في تحميل الملف"
   → تحقق من عدم وجود بيانات تالفة أو أعمدة مفقودة

❌ "لم يتم العثور على مطابقات"
   → جرّب زيادة نافذة التطابق الزمني
   → تأكد من تطابق الأسماء (تقريباً)
```

---

## 🎯 حالات الاستخدام

### 1. المراجعة الدورية
```
استخدم الصفحة شهرياً للتحقق من:
- صحة عمليات الصرف
- عدم وجود تكرارات
- مطابقة جميع الجوائز مع البنك
```

### 2. التدقيق المالي
```
قبل إغلاق السنة المالية:
- راجع جميع الحالات ⚠️ و ❌
- تحقق من أسباب عدم المطابقة
- صدّر التقرير الكامل للمحاسب
```

### 3. معالجة الأخطاء
```
عند اكتشاف تكرار:
1. استخدم الفلاتر لعزل الحالة
2. راجع تفاصيل السجلات المكررة
3. تواصل مع القسم المالي
4. صدّر تقرير الحالات المشتبهة
```

---

## 🔒 الأمان والخصوصية

```
✓ جميع البيانات تُعالج محلياً
✓ لا يتم إرسال البيانات لخوادم خارجية
✓ الملفات المرفوعة مؤقتة ويتم حذفها بعد الجلسة
✓ التقارير المصدرة محفوظة محلياً فقط
```

---

## 📞 الدعم

في حال واجهت أي مشكلة:
1. تأكد من تحديث المكتبات المطلوبة
2. راجع رسائل الخطأ في الشاشة
3. تحقق من صيغة الملفات المرفوعة
4. اتصل بفريق الدعم التقني

---

## 🚀 تحديثات مستقبلية

### قيد التطوير:
- ✨ دعم تحليل متعدد السنوات
- ✨ تصدير تقارير PDF
- ✨ لوحة تحكم تفاعلية
- ✨ إشعارات تلقائية للتكرارات
- ✨ تكامل مع أنظمة المحاسبة

---

**تم التطوير بواسطة:** GitHub Copilot  
**التاريخ:** نوفمبر 2025  
**الإصدار:** 1.0.0
