# محلل البيانات المالية المتقدم 📊

نظام متكامل لمراجعة وتدقيق الدفعات المالية مع كشف التكرارات والانحرافات باستخدام تقنيات متقدمة.

## ✨ الميزات الرئيسية

### 📥 تحميل ذكي للبيانات
- دعم ملفات Excel (xlsx, xls) و CSV
- تنظيف تلقائي للبيانات
- كشف أنواع الأعمدة تلقائياً
- معالجة الترميزات المختلفة (UTF-8, CP1256)

### 🔍 كشف التكرارات
- **تطابق تام**: كشف الدفعات المتطابقة تماماً
- **تطابق ضبابي**: كشف الأسماء المتشابهة (90%+ تطابق)
- **تطابق جزئي**: كشف التطابق في بعض الحقول
- **نافذة زمنية**: تحديد التكرارات خلال فترة معينة

### 📊 كشف الانحرافات والشذوذات
- **IQR Method**: كشف القيم الخارجة عن النطاق الطبيعي
- **Z-Score**: كشف الانحرافات الإحصائية
- **Isolation Forest**: تعلم آلي لكشف الشذوذات
- **DBSCAN**: تحليل عنقودي متقدم

### 📈 تحليلات إحصائية شاملة
- إحصائيات وصفية تفصيلية
- مقارنات بين المجموعات
- رسوم بيانية تفاعلية
- تحليل التوزيعات

### 📄 تقارير احترافية
- تصدير Excel بتنسيق احترافي
- تقارير PDF مفصلة (قريباً)
- تصدير HTML تفاعلي (قريباً)

## 🚀 التثبيت والإعداد

### المتطلبات الأساسية
- Python 3.8 أو أحدث
- pip (مدير حزم Python)

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone https://github.com/zahermasloub/Data_Analest.git
cd Data_Analest
```

2. **إنشاء بيئة افتراضية** (موصى به)
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

3. **تثبيت المتطلبات**
```bash
pip install -r requirements.txt
```

## 💻 الاستخدام

### تشغيل الواجهة التفاعلية

```bash
streamlit run app.py
```

سيفتح المتصفح تلقائياً على `http://localhost:8501`

### الاستخدام البرمجي (API)

#### مثال 1: تحميل وتنظيف البيانات

```python
from core.data_loader import DataLoader

# تحميل الملف
loader = DataLoader('data.xlsx')
loader.load()
loader.auto_clean()

# الحصول على البيانات المنظفة
df = loader.get_data()
print(f"تم تحميل {len(df)} صف")
```

#### مثال 2: كشف التكرارات

```python
from core.duplicate_analyzer import DuplicateAnalyzer

analyzer = DuplicateAnalyzer(df)
duplicates = analyzer.find_payment_duplicates(
    entity_col='اسم الجهة',
    amount_col='المبلغ',
    event_col='الحدث'
)

print(f"عدد التكرارات: {len(duplicates)}")
analyzer.export_duplicates('duplicates_report.xlsx')
```

#### مثال 3: كشف الانحرافات

```python
from core.anomaly_detector import AnomalyDetector

detector = AnomalyDetector(df)

# باستخدام IQR
anomalies = detector.detect_iqr_anomalies('المبلغ', multiplier=1.5)

# باستخدام Z-Score
anomalies = detector.detect_zscore_anomalies('المبلغ', threshold=3.0)

# باستخدام جميع الطرق
results = detector.detect_all_anomalies('المبلغ')

detector.export_anomalies('anomalies_report.xlsx')
```

## 📁 هيكل المشروع

```
Data_Analest/
│
├── app.py                      # الواجهة الرئيسية (Streamlit)
├── config.py                   # الإعدادات المركزية
├── requirements.txt            # المتطلبات
├── README.md                   # هذا الملف
│
├── core/                       # المكونات الأساسية
│   ├── __init__.py
│   ├── data_loader.py         # محرك تحميل البيانات
│   ├── duplicate_analyzer.py  # محلل التكرارات
│   └── anomaly_detector.py    # محلل الانحرافات
│
├── utils/                      # أدوات مساعدة
│   └── __init__.py
│
├── templates/                  # قوالب التقارير
├── uploads/                    # الملفات المرفوعة
└── outputs/                    # التقارير المُولّدة
```

## 🎯 حالات الاستخدام

### 1. مراجعة دفعات المشتريات
```python
# تحميل بيانات المشتريات
loader = DataLoader('purchases.xlsx')
df = loader.load().auto_clean().get_data()

# كشف الدفعات المكررة
analyzer = DuplicateAnalyzer(df)
duplicates = analyzer.find_payment_duplicates(
    entity_col='اسم المورد',
    amount_col='قيمة الفاتورة',
    date_col='تاريخ الدفع',
    time_window_days=30  # خلال 30 يوم
)

print(f"عدد الدفعات المكررة: {len(duplicates)}")
```

### 2. مراجعة رواتب الموظفين
```python
# كشف الانحرافات في الرواتب
detector = AnomalyDetector(df)
anomalies = detector.detect_iqr_anomalies('الراتب', multiplier=1.5)

# مقارنة بين الأقسام
comparison = detector.compare_groups('الراتب', 'القسم')
print(comparison)
```

### 3. تدقيق المصروفات
```python
# كشف شامل
results = detector.detect_all_anomalies('المبلغ')

# تحليل إحصائي
stats = detector.get_statistical_summary('المبلغ')
print(f"المتوسط: {stats['mean']:,.2f}")
print(f"الوسيط: {stats['median']:,.2f}")
print(f"الانحراف المعياري: {stats['std']:,.2f}")
```

## ⚙️ الإعدادات المتقدمة

يمكن تخصيص الإعدادات في ملف `config.py`:

```python
# إعدادات كشف التكرارات
DUPLICATE_DETECTION = {
    'exact_match': True,
    'fuzzy_match': True,
    'fuzzy_threshold': 90,  # نسبة التطابق المطلوبة
    'time_window_days': None,  # None = لا يوجد تقييد زمني
}

# إعدادات كشف الشذوذات
ANOMALY_DETECTION = {
    'methods': ['iqr', 'zscore', 'isolation_forest'],
    'iqr_multiplier': 1.5,
    'zscore_threshold': 3,
    'sensitivity': 'medium',  # low, medium, high
}
```

## 📊 أمثلة على النتائج

### كشف التكرارات
```
تم العثور على 292 دفعة مكررة في 146 مجموعة
نسبة التكرار: 1.02%
```

### كشف الانحرافات
```
طريقة IQR: 3,609 شذوذ (12.6%)
طريقة Z-Score: 463 شذوذ (1.6%)
Isolation Forest: 2,864 شذوذ (10.0%)
```

## 🤝 المساهمة

نرحب بجميع المساهمات! يرجى:

1. عمل Fork للمشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## 📝 الترخيص

هذا المشروع مرخص تحت رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل

## 👨‍💻 المطور

**Zaher Masloub**
- GitHub: [@zahermasloub](https://github.com/zahermasloub)

## 🙏 شكر وتقدير

شكراً لاستخدامك محلل البيانات المالية المتقدم!

---

**ملاحظة**: هذا المشروع في تطوير مستمر. نرحب بملاحظاتكم واقتراحاتكم.
