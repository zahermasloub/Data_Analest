# دليل البدء السريع - محلل البيانات المالية 🚀

## التثبيت السريع

### 1. تثبيت المكتبات
```powershell
pip install -r requirements.txt
```

### 2. تشغيل التطبيق
```powershell
streamlit run app.py
```

## الاستخدام السريع

### من خلال الواجهة التفاعلية:

1. **رفع الملف**: اضغط "Browse files" وارفع ملف Excel/CSV
2. **انتظر التحميل**: سيتم تنظيف البيانات تلقائياً
3. **اختر التحليل**: حدد نوع التحليل المطلوب
4. **احصل على النتائج**: عرض وتصدير النتائج

### من خلال الكود:

#### مثال كامل - تحليل الدفعات المكررة

```python
from core.data_loader import DataLoader
from core.duplicate_analyzer import DuplicateAnalyzer

# 1. تحميل البيانات
loader = DataLoader('بياناتك.xlsx')
df = loader.load().auto_clean().get_data()

# 2. كشف التكرارات
analyzer = DuplicateAnalyzer(df)
duplicates = analyzer.find_payment_duplicates(
    entity_col='اسم الجهة',
    amount_col='المبلغ'
)

# 3. عرض النتائج
print(f"عدد التكرارات: {len(duplicates)}")

# 4. تصدير
analyzer.export_duplicates('تقرير_التكرارات.xlsx')
```

#### مثال - كشف الانحرافات

```python
from core.data_loader import DataLoader
from core.anomaly_detector import AnomalyDetector

# تحميل وتحليل
loader = DataLoader('بياناتك.xlsx')
df = loader.load().auto_clean().get_data()

detector = AnomalyDetector(df)

# كشف بطريقة IQR
anomalies = detector.detect_iqr_anomalies('المبلغ')
print(f"عدد الشذوذات: {len(anomalies)}")

# إحصائيات
stats = detector.get_statistical_summary('المبلغ')
print(f"المتوسط: {stats['mean']:,.0f}")
print(f"الوسيط: {stats['median']:,.0f}")
```

## الأعمدة المطلوبة

### لكشف التكرارات:
- ✅ عمود الجهة/الاسم (نص)
- ✅ عمود المبلغ (رقم)
- 🔷 عمود الحدث (نص - اختياري)
- 🔷 عمود التاريخ (تاريخ - اختياري)

### لكشف الانحرافات:
- ✅ عمود رقمي للتحليل (مبلغ، راتب، إلخ)

## نصائح للاستخدام الأمثل

### 📊 جودة البيانات:
- تأكد من وجود عناوين للأعمدة في الصف الأول
- استخدم أسماء واضحة للأعمدة
- تجنب الأعمدة الفارغة تماماً

### 🔍 كشف التكرارات:
- حدد الأعمدة الأساسية بدقة
- استخدم النافذة الزمنية إن كانت مهمة
- راجع التطابق الضبابي للأسماء المتشابهة

### 📈 كشف الانحرافات:
- ابدأ بطريقة IQR (الأفضل للبيانات المالية)
- استخدم Z-Score للبيانات الموزعة طبيعياً
- جرب Isolation Forest للبيانات المعقدة

## حل المشكلات الشائعة

### ❌ خطأ في قراءة الملف:
```python
# جرب ترميزات مختلفة
loader = DataLoader('file.csv')
loader.load(encoding='cp1256')  # للملفات العربية القديمة
```

### ❌ أعمدة مكررة:
```python
# سيتم التعامل معها تلقائياً
loader.clean_column_names()  # يضيف أرقام للأعمدة المكررة
```

### ❌ قيم فارغة كثيرة:
```python
# إزالة تلقائية
loader.remove_empty_rows_and_columns(threshold=0.9)  # 90% فراغ
```

## أمثلة حسب الحالة

### 🏢 مراجعة المشتريات:
```python
duplicates = analyzer.find_payment_duplicates(
    entity_col='المورد',
    amount_col='قيمة الفاتورة',
    date_col='التاريخ',
    time_window_days=30  # خلال شهر
)
```

### 👥 مراجعة الرواتب:
```python
# كشف الرواتب غير المعتادة
anomalies = detector.detect_iqr_anomalies('الراتب', multiplier=2.0)

# مقارنة بين الأقسام
comparison = detector.compare_groups('الراتب', 'القسم')
```

### 💳 مراجعة المصروفات:
```python
# تحليل شامل
results = detector.detect_all_anomalies('المبلغ')

# عرض الإحصائيات
for method, anomalies in results.items():
    print(f"{method}: {len(anomalies)} شذوذ")
```

## الدعم والمساعدة

- 📖 الدليل الكامل: `README.md`
- 🐛 المشاكل: [GitHub Issues](https://github.com/zahermasloub/Data_Analest/issues)
- 💬 الأسئلة: افتح Discussion في GitHub

## تحديثات قادمة

- [ ] تقارير PDF احترافية
- [ ] تحليلات تنبؤية
- [ ] دعم قواعد البيانات
- [ ] API REST
- [ ] لوحة تحكم متقدمة

---

**تم التطوير بواسطة**: Zaher Masloub
**الترخيص**: MIT
