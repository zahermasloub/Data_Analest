# تعليمات التثبيت والتشغيل 📋

## المتطلبات الأساسية ✅

- Windows 10/11
- Python 3.8 أو أحدث
- اتصال بالإنترنت (للتثبيت الأول)
- 2 GB مساحة حرة على الأقل

## التثبيت السريع 🚀

### الطريقة 1: تشغيل تلقائي (الأسهل)

1. افتح مجلد المشروع
2. انقر نقراً مزدوجاً على `run.bat`
3. سيتم تثبيت المكتبات وتشغيل التطبيق تلقائياً

### الطريقة 2: يدوي (للمطورين)

#### خطوة 1: فتح PowerShell أو CMD

```powershell
cd "e:\PROGRAMS\Data_Analest"
```

#### خطوة 2: إنشاء بيئة افتراضية (موصى به)

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

**ملاحظة**: إذا ظهرت رسالة خطأ في PowerShell:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

#### خطوة 3: تثبيت المكتبات

```powershell
pip install -r requirements.txt
```

قد يستغرق هذا 2-5 دقائق حسب سرعة الإنترنت.

#### خطوة 4: التشغيل

```powershell
streamlit run app.py
```

سيفتح المتصفح تلقائياً على: `http://localhost:8501`

## اختبار النظام 🧪

لاختبار النظام على البيانات الموجودة:

```powershell
python test_system.py
```

## حل المشكلات الشائعة 🔧

### مشكلة 1: Python غير معروف

**الخطأ**: `'python' is not recognized...`

**الحل**:
1. قم بتثبيت Python من: https://www.python.org/downloads/
2. تأكد من تفعيل "Add Python to PATH" أثناء التثبيت

### مشكلة 2: فشل تثبيت المكتبات

**الخطأ**: `ERROR: Could not install packages...`

**الحل**:
```powershell
# تحديث pip
python -m pip install --upgrade pip

# إعادة المحاولة
pip install -r requirements.txt
```

### مشكلة 3: خطأ في Execution Policy

**الخطأ**: `execution of scripts is disabled...`

**الحل**:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### مشكلة 4: المنفذ مشغول

**الخطأ**: `Address already in use`

**الحل**:
```powershell
streamlit run app.py --server.port 8502
```

### مشكلة 5: خطأ في قراءة الملف العربي

**الحل**: تأكد أن الملف محفوظ بترميز UTF-8

## التحديثات 🔄

لتحديث المشروع:

```powershell
git pull origin master
pip install -r requirements.txt --upgrade
```

## إيقاف التطبيق ⏹️

للإيقاف أثناء التشغيل:
- اضغط `Ctrl + C` في النافذة
- أو أغلق نافذة PowerShell/CMD

## نصائح للأداء الأفضل ⚡

1. **استخدم بيئة افتراضية**: لتجنب تعارضات المكتبات
2. **أغلق البرامج الثقيلة**: أثناء معالجة ملفات كبيرة
3. **استخدم SSD**: للملفات الكبيرة جداً
4. **حجم الملف المثالي**: أقل من 100 MB

## الدعم الفني 💬

إذا واجهت مشكلة:

1. راجع [QUICKSTART.md](QUICKSTART.md)
2. راجع [README.md](README.md)
3. افتح Issue في GitHub
4. تواصل مع المطور

## إلغاء التثبيت 🗑️

لإزالة المشروع:

```powershell
# حذف البيئة الافتراضية
rmdir /s venv

# حذف المجلد الكامل
cd ..
rmdir /s Data_Analest
```

---

**تم إعداد هذا الدليل بواسطة**: Zaher Masloub
**آخر تحديث**: نوفمبر 2025
